import 'package:flutter/material.dart';

class WeightDrinkPages extends StatelessWidget {
  const WeightDrinkPages({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}